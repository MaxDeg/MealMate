// src/slices/users/handler.gleam
// HTTP handlers for users

pub fn mount(_router, _pool) {
  // PUT /users/:id/preferences -> update_preferences_handler
  io.println("TODO: mount users routes on router")
}

pub fn update_preferences_handler(_req, _pool) {
  io.println("TODO: implement update_preferences_handler")
}
